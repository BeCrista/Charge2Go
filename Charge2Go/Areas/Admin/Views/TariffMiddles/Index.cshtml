@model IEnumerable<Charge2Go.Models.TariffMiddle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TariffPriceImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TariffPriceDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TariffScheduleImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TariffScheduleDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TariffCampaignImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TariffCampaignDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    <img src="@item.TariffPriceImage" alt="Slider Image" class="img-fluid" style="max-width: 200px;" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TariffPriceDescription)
            </td>
            <td>
                    <img src="@item.TariffScheduleImage" alt="Slider Image" class="img-fluid" style="max-width: 200px;" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TariffScheduleDescription)
            </td>
            <td>
                    <img src="@item.TariffCampaignImage" alt="Slider Image" class="img-fluid" style="max-width: 200px;" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TariffCampaignDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
