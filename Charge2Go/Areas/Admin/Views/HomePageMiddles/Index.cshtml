@model IEnumerable<Charge2Go.Models.HomePageMiddle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageMiddle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageMiddleTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageMiddleSubtitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    <img src="@item.ImageMiddle" alt="Slider Image" class="img-fluid" style="max-width: 200px;" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageMiddleTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageMiddleSubtitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
